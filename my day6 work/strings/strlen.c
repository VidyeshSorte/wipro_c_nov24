
//Find length of string
#include <stdio.h>
#include <string.h>

int main() {
    char str[] = "Hello, nice to meet you!";
    printf("The length of the string \"%s\" is: %lu\n", str, strlen(str));
    return 0;
}